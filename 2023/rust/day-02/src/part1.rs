use std::io::Error;

pub fn process(_input: &str) -> Result<(), Error> {
    todo!("day 01 - part 1");
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_process() {
        todo!("haven't built test yet");
        let input = "";
        assert_eq!("", "");
    }
}
